---
title: useMount
description: Runs logic once when the component mounts.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-mount/useMountDemo'
import source from '../demos/use-mount/useMountDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useMount"
  description="Runs logic once when the component mounts."
  importCode={`import { useMount } from '@kamod-ch/hooks'`}
  signature={signatures.useMount}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
