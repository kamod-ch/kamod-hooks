---
title: useThrottleEffect
description: Runs an effect at throttled intervals for dependency changes.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-throttle-effect/useThrottleEffectDemo'
import source from '../demos/use-throttle-effect/useThrottleEffectDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useThrottleEffect"
  description="Runs an effect at throttled intervals for dependency changes."
  importCode={`import { useThrottleEffect } from '@kamod-ch/hooks'`}
  signature={signatures.useThrottleEffect}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
