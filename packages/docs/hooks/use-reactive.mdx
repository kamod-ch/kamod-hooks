---
title: useReactive
description: Reactive proxy object that re-renders on mutation.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-reactive/useReactiveDemo'
import source from '../demos/use-reactive/useReactiveDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useReactive"
  description="Reactive proxy object that re-renders on mutation."
  importCode={`import { useReactive } from '@kamod-ch/hooks'`}
  signature={signatures.useReactive}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
