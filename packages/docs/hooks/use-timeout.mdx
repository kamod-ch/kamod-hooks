---
title: useTimeout
description: Declarative timeout tied to component lifetime.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-timeout/useTimeoutDemo'
import source from '../demos/use-timeout/useTimeoutDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useTimeout"
  description="Declarative timeout tied to component lifetime."
  importCode={`import { useTimeout } from '@kamod-ch/hooks'`}
  signature={signatures.useTimeout}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
