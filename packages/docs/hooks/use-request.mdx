---
title: useRequest
description: Async request lifecycle with loading, data, error, refresh, and plugins.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-request/useRequestDemo'
import source from '../demos/use-request/useRequestDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useRequest"
  description="Async request lifecycle with loading, data, error, refresh, and plugins."
  importCode={`import { useRequest } from '@kamod-ch/hooks'`}
  signature={signatures.useRequest}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
