---
title: useRafState
description: State updates batched to the next animation frame.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-raf-state/useRafStateDemo'
import source from '../demos/use-raf-state/useRafStateDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useRafState"
  description="State updates batched to the next animation frame."
  importCode={`import { useRafState } from '@kamod-ch/hooks'`}
  signature={signatures.useRafState}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
