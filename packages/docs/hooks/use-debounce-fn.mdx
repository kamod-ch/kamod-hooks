---
title: useDebounceFn
description: Returns a debounced function with run, cancel, and flush.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-debounce-fn/useDebounceFnDemo'
import source from '../demos/use-debounce-fn/useDebounceFnDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useDebounceFn"
  description="Returns a debounced function with run, cancel, and flush."
  importCode={`import { useDebounceFn } from '@kamod-ch/hooks'`}
  signature={signatures.useDebounceFn}
  source={source}
  browserOnly={false}
  related={[{"name":"useDebounce","href":"/hooks/use-debounce"},{"name":"useThrottleFn","href":"/hooks/use-throttle-fn"}]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
