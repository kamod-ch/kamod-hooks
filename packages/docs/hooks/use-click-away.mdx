---
title: useClickAway
description: Calls a handler when interaction happens outside the target.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-click-away/useClickAwayDemo'
import source from '../demos/use-click-away/useClickAwayDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useClickAway"
  description="Calls a handler when interaction happens outside the target."
  importCode={`import { useClickAway } from '@kamod-ch/hooks'`}
  signature={signatures.useClickAway}
  source={source}
  browserOnly={true}
  related={[]}
  ssr={"This hook touches browser APIs. Render it after mount or guard it with BrowserOnly in SSR and static builds."}
  browser={"Requires the corresponding browser API to exist in the current environment."}
>
  <Demo />
</HookDoc>
