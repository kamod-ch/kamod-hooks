---
title: useFusionTable
description: Fusion table helper with pagination and search state.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-fusion-table/useFusionTableDemo'
import source from '../demos/use-fusion-table/useFusionTableDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useFusionTable"
  description="Fusion table helper with pagination and search state."
  importCode={`import { useFusionTable } from '@kamod-ch/hooks'`}
  signature={signatures.useFusionTable}
  source={source}
  browserOnly={false}
  related={[{"name":"usePagination","href":"/hooks/use-pagination"},{"name":"useAntdTable","href":"/hooks/use-antd-table"}]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
