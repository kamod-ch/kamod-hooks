---
title: useRafTimeout
description: Timeout scheduled through requestAnimationFrame.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-raf-timeout/useRafTimeoutDemo'
import source from '../demos/use-raf-timeout/useRafTimeoutDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useRafTimeout"
  description="Timeout scheduled through requestAnimationFrame."
  importCode={`import { useRafTimeout } from '@kamod-ch/hooks'`}
  signature={signatures.useRafTimeout}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
