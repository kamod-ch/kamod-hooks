---
title: useThrottleFn
description: Returns a throttled function wrapper.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-throttle-fn/useThrottleFnDemo'
import source from '../demos/use-throttle-fn/useThrottleFnDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useThrottleFn"
  description="Returns a throttled function wrapper."
  importCode={`import { useThrottleFn } from '@kamod-ch/hooks'`}
  signature={signatures.useThrottleFn}
  source={source}
  browserOnly={false}
  related={[{"name":"useThrottle","href":"/hooks/use-throttle"},{"name":"useDebounceFn","href":"/hooks/use-debounce-fn"}]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
