---
title: useWhyDidYouUpdate
description: Logs prop differences to the console for debugging.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-why-did-you-update/useWhyDidYouUpdateDemo'
import source from '../demos/use-why-did-you-update/useWhyDidYouUpdateDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useWhyDidYouUpdate"
  description="Logs prop differences to the console for debugging."
  importCode={`import { useWhyDidYouUpdate } from '@kamod-ch/hooks'`}
  signature={signatures.useWhyDidYouUpdate}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
