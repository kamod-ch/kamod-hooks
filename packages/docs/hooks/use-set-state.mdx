---
title: useSetState
description: Object state merging like class setState.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-set-state/useSetStateDemo'
import source from '../demos/use-set-state/useSetStateDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useSetState"
  description="Object state merging like class setState."
  importCode={`import { useSetState } from '@kamod-ch/hooks'`}
  signature={signatures.useSetState}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
