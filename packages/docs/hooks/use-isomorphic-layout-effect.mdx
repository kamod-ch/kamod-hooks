---
title: useIsomorphicLayoutEffect
description: useLayoutEffect in the browser and useEffect on the server.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-isomorphic-layout-effect/useIsomorphicLayoutEffectDemo'
import source from '../demos/use-isomorphic-layout-effect/useIsomorphicLayoutEffectDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useIsomorphicLayoutEffect"
  description="useLayoutEffect in the browser and useEffect on the server."
  importCode={`import { useIsomorphicLayoutEffect } from '@kamod-ch/hooks'`}
  signature={signatures.useIsomorphicLayoutEffect}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
