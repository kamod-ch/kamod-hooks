---
title: useHistoryTravel
description: Undo and redo history for a value.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-history-travel/useHistoryTravelDemo'
import source from '../demos/use-history-travel/useHistoryTravelDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useHistoryTravel"
  description="Undo and redo history for a value."
  importCode={`import { useHistoryTravel } from '@kamod-ch/hooks'`}
  signature={signatures.useHistoryTravel}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
