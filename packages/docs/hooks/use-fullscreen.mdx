---
title: useFullscreen
description: Controls fullscreen mode for a specific target element.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-fullscreen/useFullscreenDemo'
import source from '../demos/use-fullscreen/useFullscreenDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useFullscreen"
  description="Controls fullscreen mode for a specific target element."
  importCode={`import { useFullscreen } from '@kamod-ch/hooks'`}
  signature={signatures.useFullscreen}
  source={source}
  browserOnly={true}
  related={[]}
  ssr={"This hook touches browser APIs. Render it after mount or guard it with BrowserOnly in SSR and static builds."}
  browser={"Requires the corresponding browser API to exist in the current environment."}
>
  <Demo />
</HookDoc>
