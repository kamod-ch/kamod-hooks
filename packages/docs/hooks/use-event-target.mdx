---
title: useEventTarget
description: Builds value/onChange pairs from event-like targets.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-event-target/useEventTargetDemo'
import source from '../demos/use-event-target/useEventTargetDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useEventTarget"
  description="Builds value/onChange pairs from event-like targets."
  importCode={`import { useEventTarget } from '@kamod-ch/hooks'`}
  signature={signatures.useEventTarget}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
