---
title: useFavicon
description: Sets the current document favicon.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-favicon/useFaviconDemo'
import source from '../demos/use-favicon/useFaviconDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useFavicon"
  description="Sets the current document favicon."
  importCode={`import { useFavicon } from '@kamod-ch/hooks'`}
  signature={signatures.useFavicon}
  source={source}
  browserOnly={true}
  related={[]}
  ssr={"This hook touches browser APIs. Render it after mount or guard it with BrowserOnly in SSR and static builds."}
  browser={"Requires the corresponding browser API to exist in the current environment."}
>
  <Demo />
</HookDoc>
