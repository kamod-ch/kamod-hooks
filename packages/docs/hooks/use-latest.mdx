---
title: useLatest
description: Returns a ref that always points at the latest value.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-latest/useLatestDemo'
import source from '../demos/use-latest/useLatestDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useLatest"
  description="Returns a ref that always points at the latest value."
  importCode={`import { useLatest } from '@kamod-ch/hooks'`}
  signature={signatures.useLatest}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
