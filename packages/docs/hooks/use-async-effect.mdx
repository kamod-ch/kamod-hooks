---
title: useAsyncEffect
description: Runs an async effect and supports async cleanup.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-async-effect/useAsyncEffectDemo'
import source from '../demos/use-async-effect/useAsyncEffectDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useAsyncEffect"
  description="Runs an async effect and supports async cleanup."
  importCode={`import { useAsyncEffect } from '@kamod-ch/hooks'`}
  signature={signatures.useAsyncEffect}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
