---
title: useScroll
description: Reads scroll position from an element or document.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-scroll/useScrollDemo'
import source from '../demos/use-scroll/useScrollDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useScroll"
  description="Reads scroll position from an element or document."
  importCode={`import { useScroll } from '@kamod-ch/hooks'`}
  signature={signatures.useScroll}
  source={source}
  browserOnly={true}
  related={[]}
  ssr={"This hook touches browser APIs. Render it after mount or guard it with BrowserOnly in SSR and static builds."}
  browser={"Requires the corresponding browser API to exist in the current environment."}
>
  <Demo />
</HookDoc>
