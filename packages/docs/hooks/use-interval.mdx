---
title: useInterval
description: Declarative interval tied to component lifetime.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-interval/useIntervalDemo'
import source from '../demos/use-interval/useIntervalDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useInterval"
  description="Declarative interval tied to component lifetime."
  importCode={`import { useInterval } from '@kamod-ch/hooks'`}
  signature={signatures.useInterval}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
