---
title: useResetState
description: State with a reset function for the initial value.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-reset-state/useResetStateDemo'
import source from '../demos/use-reset-state/useResetStateDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useResetState"
  description="State with a reset function for the initial value."
  importCode={`import { useResetState } from '@kamod-ch/hooks'`}
  signature={signatures.useResetState}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
