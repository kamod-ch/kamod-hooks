---
title: useMouse
description: Tracks pointer coordinates relative to the page and an element.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-mouse/useMouseDemo'
import source from '../demos/use-mouse/useMouseDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useMouse"
  description="Tracks pointer coordinates relative to the page and an element."
  importCode={`import { useMouse } from '@kamod-ch/hooks'`}
  signature={signatures.useMouse}
  source={source}
  browserOnly={true}
  related={[]}
  ssr={"This hook touches browser APIs. Render it after mount or guard it with BrowserOnly in SSR and static builds."}
  browser={"Requires the corresponding browser API to exist in the current environment."}
>
  <Demo />
</HookDoc>
