---
title: useSelections
description: Multi-select helpers for a collection.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-selections/useSelectionsDemo'
import source from '../demos/use-selections/useSelectionsDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useSelections"
  description="Multi-select helpers for a collection."
  importCode={`import { useSelections } from '@kamod-ch/hooks'`}
  signature={signatures.useSelections}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
