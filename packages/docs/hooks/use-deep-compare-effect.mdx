---
title: useDeepCompareEffect
description: Runs an effect when deep-equal dependencies actually change.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-deep-compare-effect/useDeepCompareEffectDemo'
import source from '../demos/use-deep-compare-effect/useDeepCompareEffectDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useDeepCompareEffect"
  description="Runs an effect when deep-equal dependencies actually change."
  importCode={`import { useDeepCompareEffect } from '@kamod-ch/hooks'`}
  signature={signatures.useDeepCompareEffect}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
