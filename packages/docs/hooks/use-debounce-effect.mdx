---
title: useDebounceEffect
description: Runs an effect after dependency changes settle.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-debounce-effect/useDebounceEffectDemo'
import source from '../demos/use-debounce-effect/useDebounceEffectDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useDebounceEffect"
  description="Runs an effect after dependency changes settle."
  importCode={`import { useDebounceEffect } from '@kamod-ch/hooks'`}
  signature={signatures.useDebounceEffect}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
