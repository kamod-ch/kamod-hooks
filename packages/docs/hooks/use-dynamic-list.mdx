---
title: useDynamicList
description: Mutable-feeling list helpers for insert, remove, move, and reset.
---

import { HookDoc } from '../components/HookDoc'
import Demo from '../demos/use-dynamic-list/useDynamicListDemo'
import source from '../demos/use-dynamic-list/useDynamicListDemo.tsx?raw'
import { signatures } from '../generated/hookData'

<HookDoc
  name="useDynamicList"
  description="Mutable-feeling list helpers for insert, remove, move, and reset."
  importCode={`import { useDynamicList } from '@kamod-ch/hooks'`}
  signature={signatures.useDynamicList}
  source={source}
  browserOnly={false}
  related={[]}
  ssr={"This hook is safe to import during SSR. If your effect body touches the DOM, keep that work inside the effect callback."}
  browser={"Works in any modern browser supported by Preact."}
>
  <Demo />
</HookDoc>
